{% extends 'template.html.twig' %}
{% block title %}Annonces {{ rub.libelle }}{% endblock %}
{% block body %}
    {% include 'header.html.twig' %}
    <div class="container mb-4">
        <div class="row justify-content-center">
            {#                        {{ dump(annonces) }}#}
            {% if annonces is not empty %}
            {% for annonce in annonces %}
            <div class="col-md-4 py-2">
                {#                        <div class="card  shadow h-100 row m-auto" style="max-width: 540px;">#}
                {#                            <div class="row #}{#no-gutters#}{# m-auto">#}
                {#                                <div class="col-md-6 m-auto">#}
                {#                                    {% if annonce.imgs|first %}#}
                {#                                        <img src="{{ annonce.imgs|first }}" class="card-img " alt="..." style="max-height: 500px">#}
                {#                                    {% else %}#}
                {#                                        <img src="img/600px-No_image_available.svg.png" class="card-img img-fluid" alt="..." style="max-height: 500px">#}
                {#                                    {% endif %}#}
                {#                                </div>#}
                {#                                <div class="col-md-6">#}
                {#                                    <div class="card-body d-flex flex-column">#}
                {#                                        <h5 class="card-title">{{ annonce.entete }}</h5>#}
                {#                                        <p class="card-text"><small class="text-muted">Mise à jour#}
                {#                                                le {{ annonce.dateModif|date('d/m/Y') }}</small></p>#}
                {#                                        <a id="" class="btn btn-primary align-self-end mt-auto" href="?action=annonceUnique&param={{ annonce.annonceId }}" role="button">Consulter</a>#}
                {#                                    </div>#}
                {#                                </div>#}
                {#                            </div>#}
                {#                        </div>#}
                <div class="card shadow h-100 text-center bg-lightblue" {#style="width: 18rem;"#}>
                {% if annonce.imgs|first %}
                    <img src="{{ annonce.imgs|first }}" class="card-img-top" alt="..." style="max-height: 200px">
                {% else %}
                    <img src="img/600px-No_image_available.svg.png" class="card-img-top" alt="..."
                         style="max-height: 200px">
                {% endif %}

                <div class="card-body d-flex flex-column">
                    <h5 class="card-title">{{ annonce.entete }}</h5>
                    <p class="card-text"><small class="text-muted">Mise à jour
                            le {{ annonce.dateModif|date('d/m/Y') }}</small></p>
                    <a id="" class="btn btn-primary mt-auto stretched-link{#align-self-end #}" href="?action=annonceUnique&param={{ annonce.annonceId }}" role="button">Consulter</a>
                </div>
            </div>
        </div>
        {% endfor %}
        {% else %}
            <div class="alert alert-info" role="alert">
                <strong>Cette section est vide.</strong>
            </div>
        {% endif %}

    </div>
    </div>

{% endblock %}